https://cplusplus.com/reference/cstdlib/srand/ en linea
```
srand (time(NULL)); 
```

visualizar es un s o n, opcion

((argc <= 2 || argc > 3) || (argv[2][0] != 'n' && argv[2][0] != 's')) dice "si no se entregan los argumentos suficientes o el ultimo argumento no es n o s (en este caso ya sería cumplida la primera condición) se cancela todo"

tiempoMS viene de https://stackoverflow.com/questions/22387586/measuring-execution-time-of-a-function-in-c?utm_source=chatgpt.com

void (*funcion)(int*, int) y funcion(resultado, n); vienen de https://stackoverflow.com/questions/19417826/passing-function-pointer?utm_source=chatgpt.com para pasar una función a una función, sin especificar cuál va a ser ya que hay varias con parámetros similares

copiar_arreglo permite que cada algoritmo de ordenamiento trabaje siempre con el mismo conjunto de datos inicial (evitando modificar el original).

QuickSort hoare